" size of a hard tabstop
set tabstop=2

" size of an "indent"
set shiftwidth=2

" a combination of spaces and tabs are used to simulate tab stops at a width
" other than the (hard)tabstop
set softtabstop=2

" Turn tabs into spaces. This makes it easier to read the file
" for people who don't use vim.
set expandtab

" Automatic indentation.
set autoindent
